<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        if (Schema::hasTable('{{TABLE_NAME}}')) {
            return;
        }

        Schema::create('{{TABLE_NAME}}', function (Blueprint $table) {
            $table->id();
            
            // Hero Section
            $table->json('hero_title')->nullable();
            $table->json('hero_label')->nullable();
            $table->json('hero_highlighted')->nullable();
            $table->json('hero_description')->nullable();
            $table->json('hero_cta_primary')->nullable();
            $table->string('hero_cta_primary_route')->nullable();
            $table->json('hero_cta_secondary')->nullable();
            $table->string('hero_cta_secondary_route')->nullable();

            // Overview Section
            $table->json('overview_title')->nullable();
            $table->json('overview_subtitle')->nullable();
            $table->json('overview_description')->nullable();
            $table->string('overview_right_block_type')->default('none')->nullable();
            $table->json('overview_right_block_title')->nullable();
            $table->string('overview_right_block_icon')->nullable();
            $table->json('overview_right_block_description')->nullable();

            // Features / Bento Grid
            $table->json('features_title')->nullable();
            $table->json('features_description')->nullable();
            $table->json('features')->nullable();

            // FAQs
            $table->json('faqs_title')->nullable();
            $table->json('faqs_description')->nullable();
            $table->json('faqs')->nullable();

            // Footer CTA Section
            $table->json('footer_cta_title')->nullable();
            $table->json('footer_cta_button')->nullable();
            $table->string('footer_cta_route')->nullable();

            $table->timestamps();
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('{{TABLE_NAME}}');
    }
};
